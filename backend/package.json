{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "Backend Express para o gerenciador de tarefas TaskFlow",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "cd .. && npx cypress run --spec 'cypress/e2e/backend.cy.js'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
